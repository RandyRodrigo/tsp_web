---
import Button from './Button';

interface Props {
    img: string;
}

const { img } = Astro.props
---


<div  
    style={`background-image: url('${img}'); clip-path: polygon(0 0, 100% 0, 100% 85%, 50% 100%, 50% 100%, 0 85%);`} 
    class="bg-no-repeat bg-cover relative h-[100vh] w-full max-w-[100vw] animate-fade-up">
    <section class={`
        
        flex flex-col lg:flex-row gap-4
        mx-auto p-6 md:p-12 lg:p-20
        absolute top-0 left-0 right-0 bottom-0
        items-center justify-center
      `}>
        <div class="col-span-2 md:col-span-2 flex flex-col justify-center items-center">
            <h2 class="text-4xl font-semibold text-balance mb-4 text-center">Bienvenidos/as a nuestros servicios</h2>
            <p class="text-base md:text-lg max-w-xl text-center">
                Aquí puedes encontrar información sobre nuestros servicios y cómo podemos ayudarte.
            </p>
            <Button className="animate-fade-up uppercase" url="https://wa.me/+51908915072">Contacto
            </Button>
        </div>
        <div class="col-span-2 md:col-span-2 flex flex-col justify-center items-center">
            <img src="/images/PortadaDerecha.png" alt="Imagen de servicio"  />
        </div>
      </section>
</div>