---
import { CountUp } from "./CountUp";
---

<section class="max-w-6xl mx-auto py-20 px-20">
    <h2 class="text-4xl lg:text-4xl font-semibold text-center text-balance mb-10 lg:mb-20 text-rojo">
        Cotiza tu Sitio Web con TSP Negocios
    </h2>
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-y-10">
        <div class="border-left flex flex-col justify-center items-center">
            <span
                class="number text-6xl font-bold text-balance tabular-nums tracking-tighter"
            >
                +<CountUp client:visible initial={0} final={1500} />
            </span>
            <span class="uppercase opacity-70">Proyectos Entregados</span>
        </div>
        <div class="border-left flex flex-col items-center justify-center">
            <span
                class="number text-6xl font-bold text-balance tabular-nums tracking-tighter"
            >
                +<CountUp client:visible initial={0} final={1500} />
            </span>
            <span class="uppercase opacity-70">Clientes Satisfechos</span>
        </div>
        <div class="flex flex-col items-center justify-center">
            <span
                class="number text-6xl font-bold text-balance tabular-nums tracking-tighter"
            >
                +<CountUp client:visible initial={0} final={1000} />
            </span>
            <span class="uppercase opacity-70">Recomendaciones</span>
        </div>
    </div>
</section>

<style>
    .number {
        color: #BF1F17;
        text-shadow: 0 0 10px rgba(146, 0, 0, 0.50);
    }
</style>
